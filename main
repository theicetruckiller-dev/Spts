local player = game:GetService("Players").LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

-- ScreenGui & Panel
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.Name = "MobileRingAutoClicker"
gui.ResetOnSpawn = false

local panel = Instance.new("Frame", gui)
panel.BackgroundColor3 = Color3.fromRGB(40,40,60)
panel.Size = UDim2.new(0,210,0,130)
panel.Position = UDim2.new(0.04,0,0.7,0)
panel.Active = true
panel.Draggable = true

local title = Instance.new("TextLabel", panel)
title.Text = "🎯 Ring Auto Clicker"
title.Size = UDim2.new(1,0,0,28)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(190,230,255)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Position = UDim2.new(0,0,0,2)

-- Halka göster/gizle
local ringBtn = Instance.new("TextButton", panel)
ringBtn.Size = UDim2.new(0,120,0,28)
ringBtn.Position = UDim2.new(0,12,0,36)
ringBtn.BackgroundColor3 = Color3.fromRGB(90,170,180)
ringBtn.Text = "Halka Aç/Kapa"
ringBtn.TextColor3 = Color3.fromRGB(250,255,255)
ringBtn.Font = Enum.Font.GothamBold
ringBtn.TextSize = 15

-- Clicker aç/kapa
local clickerBtn = Instance.new("TextButton", panel)
clickerBtn.Size = UDim2.new(0,90,0,28)
clickerBtn.Position = UDim2.new(0,134,0,36)
clickerBtn.BackgroundColor3 = Color3.fromRGB(40,180,100)
clickerBtn.Text = "Clicker: Kapalı"
clickerBtn.TextColor3 = Color3.fromRGB(230,255,230)
clickerBtn.Font = Enum.Font.GothamBold
clickerBtn.TextSize = 13

-- Hız ayarı
local speedBox = Instance.new("TextBox", panel)
speedBox.Size = UDim2.new(0,60,0,24)
speedBox.Position = UDim2.new(0,12,0,76)
speedBox.BackgroundColor3 = Color3.fromRGB(60,70,90)
speedBox.Text = "100"
speedBox.TextColor3 = Color3.fromRGB(220,250,250)
speedBox.Font = Enum.Font.Gotham
speedBox.TextSize = 14
speedBox.ClearTextOnFocus = false

local speedLabel = Instance.new("TextLabel", panel)
speedLabel.Text = "Hız (ms)"
speedLabel.Size = UDim2.new(0,50,0,20)
speedLabel.Position = UDim2.new(0,75,0,76)
speedLabel.BackgroundTransparency = 1
speedLabel.TextColor3 = Color3.fromRGB(190,230,255)
speedLabel.Font = Enum.Font.Gotham
speedLabel.TextSize = 13

-- Infinite Jump Butonu
local jumpBtn = Instance.new("TextButton", panel)
jumpBtn.Size = UDim2.new(0,170,0,26)
jumpBtn.Position = UDim2.new(0,12,0,104)
jumpBtn.BackgroundColor3 = Color3.fromRGB(80,70,180)
jumpBtn.Text = "Infinite Jump: Kapalı"
jumpBtn.TextColor3 = Color3.fromRGB(230,230,255)
jumpBtn.Font = Enum.Font.GothamBold
jumpBtn.TextSize = 14

-- Halka (Ring)
local ring = Instance.new("ImageButton", gui)
ring.Image = "rbxassetid://6031091006"
ring.BackgroundTransparency = 1
ring.Size = UDim2.new(0,60,0,60)
ring.Position = UDim2.new(0.5, -30, 0.5, 80)
ring.Active = true
ring.Draggable = true
ring.Visible = false

-- Vars
local ringVisible = false
local running = false
local clickDelay = 0.10
local infiniteJump = false

-- Ring Göster/Gizle
ringBtn.MouseButton1Click:Connect(function()
    ringVisible = not ringVisible
    ring.Visible = ringVisible
end)

-- Clicker Başlat/Durdur
clickerBtn.MouseButton1Click:Connect(function()
    running = not running
    clickerBtn.Text = running and "Clicker: Açık" or "Clicker: Kapalı"
end)

-- Sonsuz click döngüsü (Ring’in tam ortasına!)
spawn(function()
    while true do
        if running and ringVisible then
            local spd = tonumber(speedBox.Text) or 100
            -- RING'İN EKRANDAN DÜNYAYA POZİSYONUNU AL:
            local viewportPos = ring.AbsolutePosition + (ring.AbsoluteSize/2)
            -- Mobilde "mouse1click" varsa, mouse'u ring'in üstüne çek ve tıkla:
            if mousemoverel and mouse1click then
                -- Sadece PC için: mouse'u ring'in ortasına taşır ve tıklar
                local cx, cy = workspace.CurrentCamera.ViewportSize.X, workspace.CurrentCamera.ViewportSize.Y
                local moveX = viewportPos.X - (cx/2)
                local moveY = viewportPos.Y - (cy/2)
                mousemoverel(moveX, moveY)
                wait(0.02)
                mouse1click()
                wait(0.02)
                mousemoverel(-(moveX), -(moveY))
            elseif mouse1click then
                mouse1click()
            end
            -- Eğer bunlar çalışmazsa: Halka'nın üstündeki tıklama eventini tetikle
            ring:Activate()
            wait(math.max(spd/1000,0.01))
        else
            wait(0.1)
        end
    end
end)

-- Infinite Jump (Yere her değdiğinde bir kere zıplatır, uzaya uçurmaz!)
local lastJump = tick()
RunService.RenderStepped:Connect(function()
    if infiniteJump then
        local char = player.Character
        if char and char:FindFirstChildOfClass("Humanoid") then
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid:GetState() == Enum.HumanoidStateType.Landed then
                if tick() - lastJump > 0.08 then
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    lastJump = tick()
                end
            end
        end
    end
end)

jumpBtn.MouseButton1Click:Connect(function()
    infiniteJump = not infiniteJump
    jumpBtn.Text = infiniteJump and "Infinite Jump: Açık" or "Infinite Jump: Kapalı"
end)
